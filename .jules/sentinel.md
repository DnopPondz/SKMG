## 2026-04-06 - [CRITICAL] Registration Endpoint Handled Improperly\n**Vulnerability:** Registration endpoint `src/app/api/register/route.ts` was incorrectly configured as a Mongoose schema definition, leaving user registration broken and insecure.\n**Learning:** The schema definition `ProductSchema` was accidentally misplaced in the route file while already properly defined in `src/models/Product.ts`.\n**Prevention:** Ensure route files are only exporting proper request handler functions (e.g. `POST`, `GET`).
